<template name="tab">
	<view class="tab">
		<block wx:for="{{list}}">
			<view class="{{item.active ? 'active' : ''}}">
				<navigator url="{{item.url}}">
					{{item.label}}
				</navigator>
			</view>
		</block>
	</view>
</template>